#include "main.h"
#include <stdlib.h>

/**
 * malloc_checked-allocates memory using malloc
 * @x: number of byte to allocate
 * Return: a pointer to the allocated memory
 */

void *malloc_checked(unsigned int x)
{
	void *x;

	ptr = malloc(x);
	if
	(x == NULL);
	exit(98);
	return (x);
}
